name: Deploy WASM Playground

on:
  push:
    branches: [main]
    paths:
      - 'unpdf-wasm/**'
      - 'docs/**'
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  pages:
    uses: iyulab/uncore/.github/workflows/pages-reusable.yml@main
    with:
      wasm-crate: unpdf-wasm
    permissions:
      contents: read
      pages: write
      id-token: write
